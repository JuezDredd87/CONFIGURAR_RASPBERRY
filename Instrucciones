Instalar ffmepg:
* Primero instalar docker
	curl -fsSL https://get.docker.com -o get-docker.sh
	sudo sh get-docker.sh
	sudo usermod -aG docker pi
	docker version

Instalar unrar-nonfree:
* Primero habilitar repositorios en /etc/apt/sources.list
